package com.deivitdev.peakflow

import androidx.compose.runtime.Composable

@Composable
actual fun SystemAppearance(isDark: Boolean) {
    // On iOS, status bar appearance is typically managed via Info.plist 
    // or UIViewController methods if not using a custom controller.
}
