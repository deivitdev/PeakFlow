## 1. Logic & Resources

- [x] 1.1 Implement zone bucketing logic in `ActivityDetailViewModel`
- [x] 1.2 Add training zone labels (Z1-Z5) to `strings.xml` (EN/ES)
- [x] 1.3 Define a `HeartRateZone` data model to pass to the UI

## 2. Component Implementation

- [x] 2.1 Create the `ZonesDistributionChart` using Compose and `Canvas` (or standard `Row`/`Box` components)
- [x] 2.2 Apply \"Electric Pro\" colors to each zone bar
- [x] 2.3 Add percentage and duration text labels to the chart

## 3. Screen Integration

- [x] 3.1 Add the `ZonesDistributionChart` card to the `ActivityDetailScreen` layout
- [x] 3.2 Ensure the chart only appears if heart rate data is available
- [x] 3.3 Verify that the chart correctly reflects the activity's intensity distribution
